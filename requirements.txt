ccxt>=4.0.0
scikit-learn>=1.0.0
pandas>=1.3.0
numpy>=1.21.0
telethon>=1.24.0
# NAS100 ML bot (MT5). MetaTrader5 is Windows-only; install on the same
# machine running the MT5 terminal.
MetaTrader5>=5.0.45 ; platform_system == "Windows"
